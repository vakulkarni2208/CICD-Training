<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Base_SKU__c</fullName>
    <description>Opp Reg &gt; SAP integration (it can be semi-colon separated; if a sfdc product is applicable to 2 SKUs eg., different product like 16gb|32gb but its support has same price; in opp reg scenario instead of 2 products we are having only one product as pricing remains same)</description>
    <externalId>false</externalId>
    <label>Base SKU</label>
    <length>50</length>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>true</trackHistory>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
